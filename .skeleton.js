import React from "react";
import { G } from "@/styles";
import styled from "styled-components";

const Container = styled.div``;

interface IProps {}

export const Foo: React.FC<IProps> = ({}) => {
  return (
    <>
      <Container></Container>
    </>
  );
};
